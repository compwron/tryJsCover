apply plugin: 'java'

repositories {
    mavenCentral()
}

dependencies {
    compile fileTree(dir: 'lib', include: '**/*.jar')
}

ant.importBuild 'build.xml'

task generateSer(type: JavaExec) {
    main = "com.olabini.jescov.console.Runner"
    classpath = sourceSets.main.runtimeClasspath
}